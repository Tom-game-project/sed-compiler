# ステップ1：1の補数を求める (0と1をすべて反転)
y/01/10/

# ステップ2：1を足す (二進数のインクリメント)
# 末尾に計算用の「キャリー(桁上がり)マーカー」として '+' を追加
s/$/+/

# 桁上がりを処理するループ
:add_one_loop
  # 右端が '0+' なら '1' にして終了
  s/0+$/1/
  t add_one_done

  # 右端が '1+' なら '+0' にして桁上がりを左へ伝播させる
  s/1+$/+0/
  # 桁上がりが続く可能性があるのでループ先頭へ
b add_one_loop

:add_one_done
# もし全体が桁上がりした場合 (例: 111+1 -> 1000)、先頭の '+' を '1' に変える
s/^\+/1/
